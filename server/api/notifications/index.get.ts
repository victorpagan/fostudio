// File: server/api/notifications.get.ts
export default defineEventHandler(async () => {
  // v1: return empty list. Later you can generate notifications from membership status,
  // expiring credits, upcoming bookings, etc.
  return []
})
